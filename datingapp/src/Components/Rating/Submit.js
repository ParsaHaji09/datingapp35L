import React, { useState } from 'react';
import './Submit.css';

function Submit() {
    return (
      <button className="submit"
        type="submit">
        SUBMIT
      </button>
    );
  }
  
  export default Submit;